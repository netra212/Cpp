#include <iostream>
using namespace std;

/**
 *
 * Double Pointers Multiple Pointers.
    int n = 100;
    int *p = &n;
    If we want to store the address of the pointer in this scenarios, we used the double pointer.

    int **ptr = &p;
    int ***p1 = &ptr;

    Use case of

 */

int main()
{
    //
}
